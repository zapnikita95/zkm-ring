# Used when deploying service green-route-web from repo root.
[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/web/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
