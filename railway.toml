# green-route-web (CLI `railway up` / default).
# zeleny-api must NOT use this file — see deploy/api/railway.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/web/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
